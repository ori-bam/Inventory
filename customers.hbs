<h1 align="center"> My Customers</h1>
<div class="add"><a href="/customers/new" type="button" title="Add new"><i class="fas fa-user-plus fa-2x"></i>
  </a>
</div>
<div class="container">
  <div class="row">
    <div class="col-12">
      <table class="table table-bordered">
        <thead>
          <tr align="center">
            <th scope="col">#</th>
            <th scope="col">Name</th>
            <th scope="col">Email</th>
            <th scope="col">Phone</th>
            <th scope="col">City</th>
            <th scope="col">Actions</th>
          </tr>
        </thead>
        <tbody>
          {{#each custDB}}
          <form action="/customers/edit/:id" method="post">
            <tr>
              <th scope="row">{{id}}</th>
              <td>{{name}}</td>
              <td>{{email}}</td>
              <td>{{phone}}</td>
              <td>{{city}}</td>
              <td>
                <input type="hidden" name="id" value="{{id}}" />
                <button type="submit" class="btn btn-info" name="edit" title="Edit"><i class="fas fa-edit"></i></button>
                <button type="submit" class="btn btn-warning" name="delete" title="Delete"
                  formaction="/customers/delete"><i class="fas fa-trash"></i></button>
                <button type="submit" class="btn btn-success" name="view" title="View"
                  formaction="/customers/show/:id"><i class="fas fa-eye"></i></button>
              </td>
            </tr>
          </form>
          {{/each}}
        </tbody>
      </table>
    </div>
  </div>
</div>